import styled from "styled-components";

const Heading = styled.h1.attrs({
  className: `text-5xl lg:text-3xl font-bold font-display tracking-wide my-4 tracking-wide max-w-prose leading-20` as string,
})``;

export default Heading;